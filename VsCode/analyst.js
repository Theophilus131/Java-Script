function analyst(){

const survey = [{name: theophilus, score: 80}, {name: FileSystemDirectoryHandle, score: 20}, 

    {name:abigail, score: 30}
]

const new_survey = []

for(let index = 0; index < survey.length; index++){
    survey[index].sort();
    
    new_survey.push(survey[index];
        }
return new_survey;
    }
    }